# Intent: <short name>

Captured directly from whoever originated this — a product owner, an
incident, a backlog item. This is the **Plan** stage artifact: nothing gets
designed or built from an idea that hasn't been written down first. The
`sdlc-intent` skill turns this into one or more `templates/spec.md` files;
don't skip straight to a spec.

## What problem is this solving?

Plain language. Who hits this, how often, what it costs them today.

## What does "done" look like?

Observable outcomes, not implementation. "A user can X without Y" — not
"add an endpoint for X".

## Constraints

Anything non-negotiable: a deadline, a compliance requirement, a platform
this must not touch, a decision already made elsewhere that this must not
re-open.

## Non-goals

What this explicitly does not cover, so a spec written from this doesn't
scope-creep into adjacent work.

## Origin

Who asked for this and when. Link the issue/thread/conversation this came
from — the audit trail the playbook asks for starts here.

---
*Owner sign-off required before this becomes a spec — see docs/PLAYBOOK.md
("Plan" stage gate).*
