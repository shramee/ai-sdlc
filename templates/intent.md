# Intent: <short name>

The **Plan**-stage artifact (`docs/PLAYBOOK.md`), captured from the
originator. `sdlc-intent` splits this into `templates/spec.md` files; don't
skip straight to a spec.

## What problem is this solving?

Who hits this, how often, what it costs them today.

## What does "done" look like?

Observable outcomes, not implementation — "a user can X without Y".

## Constraints

Non-negotiables: a deadline, a compliance requirement, an off-limits
platform, a decision not to re-open.

## Non-goals

What this explicitly does not cover.

## Origin

Who asked for this and when; link the source issue/thread/conversation.

---
*Owner sign-off required before this becomes a spec — `docs/PLAYBOOK.md`,
"Plan" stage gate.*
